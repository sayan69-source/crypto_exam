import SetterLayout from '@/components/layout/SetterLayout';

export default function SetterRouteLayout({ children }: { children: React.ReactNode }) {
  return <SetterLayout>{children}</SetterLayout>;
}
