# Production deploy dependency set (the LIGHT subset that actually boots the API).
# Excludes the heavy/optional libs (gmpy2, sentence-transformers, torch, celery,
# openai) — those are lazy/guarded imports and are not needed to serve the app.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0
asyncpg>=0.29.0
pyjwt[crypto]>=2.8.0
bcrypt>=4.0.0
cryptography>=42.0.0
pycryptodome>=3.20.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
httpx>=0.27.0
web3>=6.15.0
pyotp>=2.9.0
email-validator>=2.0.0
numpy>=1.26.0
pillow>=10.0.0
