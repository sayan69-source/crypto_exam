/** Types for ensure-keys.mjs — plain JS so the build scripts can run it directly. */
export declare const PUBLIC_PEM: string;
export declare const PRIVATE_PEM: string;
export declare function ensureDemoKeypair(): "created" | "present";
