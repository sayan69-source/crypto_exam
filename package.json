{
  "name": "zuup-os",
  "version": "2.0.0",
  "private": true,
  "description": "ZUUP-OS — Secure Examination Terminal OS & Multi-Portal Control Plane (monorepo root). See private/zuup_os_implementation_plan.md.",
  "workspaces": [
    "packages/*",
    "private/centre-admin",
    "private/edge-server",
    "private/system-admin"
  ],
  "scripts": {
    "check:boundary": "node scripts/check-boundary.mjs",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=22"
  }
}
